plugins {
    // auto-downloads JDK if the one specified in the toolchain is not install on local system
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'quiz'

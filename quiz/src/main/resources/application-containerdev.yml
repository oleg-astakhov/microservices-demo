spring:
  datasource:
    url: jdbc:postgresql://micros-database:5432/quiz
    driverClassName: org.postgresql.Driver
    username: microsappuser
    password: microservices-database-pwd
  rabbitmq:
    host: msg-broker-dev

logging:
  level:
    org:
      springframework:
        amqp:
          rabbit:
            core:
              RabbitAdmin: DEBUG